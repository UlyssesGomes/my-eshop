import { EllipisTooltip } from './ellipis-tooltip';

describe('EllipisTooltip', () => {
  it('should create an instance', () => {
    const directive = new EllipisTooltip();
    expect(directive).toBeTruthy();
  });
});
